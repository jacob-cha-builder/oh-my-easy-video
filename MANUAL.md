# 처음 한 편 만들어보기

설치는 [`README.md`](README.md) 에 있다. 이 문서는 **첫 영상을 실제로 한 편 뽑아보는
워크스루**다.

| 문서 | 대상 | 용도 |
|---|---|---|
| `README.md` | 사람 | 왜 필요한지, 설치, 자주 막히는 곳 |
| **이 문서** | 사람 | 처음부터 끝까지 한 번 따라해보기 |
| `skills/oh-my-easy-video/SKILL.md` | Claude | 에이전트가 따르는 절차 |

---

## 핵심은 명령어가 아니라 대화다

준비물만 갖춰두면 나머지는 Claude 에게 말을 걸면 된다. 실제 흐름의 예시:

> **나**: 우리 SOEC(고체산화물 수전해) 기술 설명 발표자료 있는데, 이걸로 2분짜리
> 설명영상 만들어줘. [슬라이드 5장 첨부]
>
> **Claude**: 자료 봤습니다. 먼저 몇 가지 확인할게요 — 이 영상은 투자자용인가요,
> 팀 내부 공유용인가요? 5장을 다 다룰까요, 핵심 3장만 추릴까요?
>
> **나**: 팀 내부용이고, 3장만.
>
> **Claude**: [`/hyperframes` 인터뷰 → `BRIEF.md`]
> 프레임 표를 만들었습니다. 보드 열어뒀으니 확인해주세요 — localhost:3002/?view=storyboard
> 프레임마다 코멘트 남기시고, 다 하시면 채팅으로 한 마디 주세요.
>
> **나**: 2번 프레임이 너무 기네. 반으로 줄여줘.
>
> **Claude**: 반영했습니다. 이제 **초안(와이어프레임)** 을 올릴게요 — 진짜 문구는 들어가되
> 디자인·모션은 아직 없습니다. 레이아웃만 봐주세요.
>
> [초안 확정 → 빌드 → 최종 리뷰 → 렌더]

**하나씩 제안하고 매번 확인받는다.** 한꺼번에 만들어서 통보하지 않는다.

## 4패스 — 각 단계에서 뭘 보는가

| 패스 | 화면에 보이는 것 | 당신이 할 일 |
|---|---|---|
| ① plan | 프레임 표 (텍스트) | 순서·길이·메시지가 맞는지 |
| ② 초안 | 회색 판때기 + 진짜 문구 | **레이아웃만** 본다. 예쁜지는 아직 보지 않는다 |
| ③ 빌드 | 디자인·모션 입힌 완성 프레임 | 의도대로 움직이는지 |
| ④ 최종 | 오디오 붙은 전체 영상 | 렌더할지 |

**②를 건너뛰지 마세요.** 여기서 레이아웃을 확정하지 않으면 ③에서 다시 그려야 한다.
초안은 몇 분이면 끝나고, 빌드는 훨씬 오래 걸린다.

## 보드에서 코멘트하기

```bash
npx hyperframes preview --background
```

브라우저에서 `http://localhost:<port>/?view=storyboard#project/<이름>`

- 프레임마다 "Comment on this frame..." 입력창이 있다
- 코멘트를 남기고 제출한다
- **제출만으로는 Claude 가 모른다** — 채팅으로 아무 말이나 한 마디 보내야 읽는다
- 화면이 갱신 안 되면 **완전 새로고침** (주소창 해시만 바뀌는 이동으로는 안 된다)

## 대본에 영어를 쓰지 않는다

한국어 음성(Piper)은 라틴문자를 읽지 못한다. 화면엔 `re0` 라고 쓰고 음성은 "리제로"라고
읽으면, 보는 사람이 둘을 머릿속에서 맞춰야 한다.

**화면과 음성에 같은 한국어를 쓴다.** 저장소 주소처럼 눈으로만 읽을 것은 화면에만 띄우고
나레이션에서는 "저장소에서 확인해보세요" 처럼 가리키기만 한다.

저장할 때 자동으로 검사되고, 걸리면 이렇게 뜬다:

```
⚠ [규칙 7] 나레이션에 라틴문자 "re0" — 한국어 용어로 바꾸거나 화면에만 띄우세요
```

## 막혔을 때

Claude 에게 그냥 **"지금 어디까지 됐지?"** 라고 물으면 된다. 셸에서 직접 보려면:

```bash
node ~/.claude/plugins/cache/oh-my-easy-video/oh-my-easy-video/*/scripts/status.mjs \
  --project <프로젝트 폴더>
```

지금 어느 단계이고 다음에 뭘 해야 하는지 알려준다.

| 상태 | 뜻 |
|---|---|
| `no-brief` | 인터뷰를 건너뛰었다 — `/hyperframes` 부터 |
| `sketch-pending` | 초안 패스를 건너뛰려 하고 있다 |
| `no-audio` | 대본은 됐고 음성이 없다 |
| `missing-words` | 단어 타임스탬프가 비었다 (캡션 쓸 거면 다시) |
| `ready-to-render` | 렌더 가능 |

## 더 읽을 거리

전부 `plugins/oh-my-easy-video/skills/oh-my-easy-video/references/` 안에 있다.

| 파일 | 내용 |
|---|---|
| `korean-narration.md` | 발화 속도, 조사, 숫자, 화면 카피 예산 |
| `korean-terminology.md` | 화면과 음성 일치 규칙 |
| `korean-typography.md` | `@font-face`, `word-break: keep-all` |
| `gates.md` | 검사 규칙 7종, TTS 상세 |
| `deck-ingest.md` | 발표자료로 만들 때 |
| `why.md` | 이 플러그인이 존재하는 이유 |
